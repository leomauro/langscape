# initial comment
# another one + two spaces


import sys  # import and line comment
import  os  # two spaces between import and os

def foo():                 # simple function and one loop
    '''
    doctstring
    '''
    for i in range(100):
        print i%5 + 3

# nested object
def bar():
    def baz():
        while open(bla):
            # line comment here
            zip(bla)
    class X(object): pass

    class Y(object):
        for i in range(20):
            assert 1/(i+1)**2

if os.path:
    0


